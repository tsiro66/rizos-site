---
import MainLayout from '../../layouts/MainLayout.astro';
import FactoryContent from '../../components/pages/FactoryContent.astro';
import { useTranslations, getStaticPaths } from '../../i18n/utils';

export { getStaticPaths };

const { locale } = Astro.params;
const { t } = useTranslations(locale!);
---

<MainLayout title={t('factory.pageTitle')} t={t} locale={locale}>
  <FactoryContent t={t} locale={locale} />
</MainLayout>
